type User = {
  id: number;
  login: string;
  password: string;
  bio: string;
};

type Book = {
  id: number;
  title: string;
  summary: string;
};

type Review = {
  id: number;
  text: string;
};
